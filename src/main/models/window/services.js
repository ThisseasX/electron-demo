export const minimize = (window) => window.minimize();
export const maximize = (window) => window.maximize();
export const unmaximize = (window) => window.unmaximize();
export const close = (window) => window.close();
